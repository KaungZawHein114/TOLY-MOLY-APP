# No models — the App Assistant is stateless per request. Conversation
# history lives only in the Flutter client's session (chatMessagesProvider)
# and is sent back on every call; nothing is persisted server-side.
