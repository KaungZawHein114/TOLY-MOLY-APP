# No models — the AI Tasker Finder is stateless per request. It classifies one
# free-text problem description into a service category and returns; nothing is
# persisted, and no tasker data is stored or read here (the tasker search runs
# entirely on the Flutter side against the local demo database).
